Алгоритм, задание 6

Часть 1

Объект "Группа" Group

Сохранить в коллекцию строки ответов для каждой группы.

Внутри объекта:
объединить строки в каждого объекта в одну линию.
взять символ из строки,
сохранить в коллекцию set, где нет повторов символов
подсчитать количетсво уникальных знаком(узнать длину коллекции)

суммировать количество  уникальных ответов каждого объекта

Часть 2

(подсчитать количетсво вопросов, на которые каждый участник группы ответил "да")

Внутри объекта:
выбрать самую длинную строку
проверить наличие ее элементов в других строках
переменная "количества текущего символа"
инкрементировать если есть

по окончанию фора(по количеству участников группы) текущую переменную на количество участников
если количества идентичны то инкрементировать переменную "все общие ответы"
иначе обнулить переменную "количество текущего символа"

суммировать количество идентичных  ответов каждого объекта


























